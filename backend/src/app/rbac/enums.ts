export enum MemberStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
    SUSPENDED = 'suspended'
}

export enum MemberRole {
    OWNER = 'owner',
    BRANCH_MANAGER = 'branch_manager',
    STAFF = 'staff'
}